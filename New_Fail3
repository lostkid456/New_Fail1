int x=300;
int y=300;

void setup(){
  size(600,600);
  background(0);
}
void draw(){
  stroke(255);
  line(0,y,600,y);
  line(x,0,x,600);
  if(mousePressed==true){
   background(0);
   x=mouseX;
   y=mouseY;
  }
  if(keyPressed){
  if(key==CODED){
    if(keyCode == UP){
       
    }
    if(keyCode== DOWN){
      
    }
  } else{
    
  }
  }
}
